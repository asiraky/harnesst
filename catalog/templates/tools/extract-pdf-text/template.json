{
  "id": "extract-pdf-text",
  "subagentCompatible": true,
  "type": "tool",
  "name": "Extract PDF Text",
  "description": "Extract embedded text from a base64-encoded PDF locally, without uploading the document or requiring a secret. Returns the text and page count, with bounded input and output; scanned or image-only PDFs are reported clearly because this tool does not perform OCR.",
  "version": "0.1.0",
  "eve": ">=0.1.0",
  "files": ["tools/extract-pdf-text.ts"],
  "assistantSkill": "assistant-skill.md",
  "dependencies": {
    "unpdf": "^1.8.0",
    "zod": "^4.4.3"
  }
}
