{
  "id": "publish-artifact",
  "type": "tool",
  "name": "Publish Artifact",
  "description": "Publish an image the agent produced (a screenshot, a chart, a diagram) into the Front of House conversation, where it renders as a card the user can see. harnesst copies the file out of the agent's home directory and serves it back behind the user's own sign-in.",
  "version": "0.1.0",
  "eve": ">=0.20.0",
  "files": [
    "tools/publish-artifact.ts"
  ],
  "assistantSkill": "assistant-skill.md",
  "dependencies": {
    "zod": "^4.4.3"
  }
}
